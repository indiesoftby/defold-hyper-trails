function spawn()
	local pos = go.get_position()
	local x = math.random(-100, 100)
	local y = math.random(-100, 100)
	local id = factory.create("#factory", vmath.vector3(pos.x + x, pos.y + y, 0))
end

function init(self)
	timer.delay(0.3, true, spawn)
end